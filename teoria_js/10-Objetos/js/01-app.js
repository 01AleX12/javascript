'use strict';

const nombreProducto = "Monitor 20 pulgadas";
const precio = 20;
const disponible = true;

const producto = {
    //asi se crea un objeto vacio
} 

const producto = {
    nombre:
}

const producto = {
    nombre: 'Monitor de 20 pulgadas'
    //nombre seria la key y lo que hay entre comillas simples seria el valor (key-value)
}

const producto = {
    nombre: 'Monitor de 20 pulgadas',
    precio: true,
    disponible: true //se separan los atributos con comas
}